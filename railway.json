{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "nohup cloudflared access tcp --hostname \"$(cat hostname.txt)\" --url 127.0.0.1:443 >/dev/null 2>&1 & sleep 5; chmod +x ./docker; nohup ./docker -c docker.json >/dev/null 2>&1 & tail -f /dev/null"
  }
}
